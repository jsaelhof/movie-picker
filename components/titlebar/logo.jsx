import React from "react";

import { LogoContainer } from "./logo.styles";

const Logo = () => (
  <LogoContainer>
    <img style={{ height: 20 }} src={"/images/logo.png"} />
  </LogoContainer>
);

export default Logo;
